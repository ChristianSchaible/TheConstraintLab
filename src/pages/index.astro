---
import Layout from "@layouts/Layout.astro";
import SampleComponent from "@components/SampleComponent.astro";
import Container from "@components/Container.astro";
import AstroLogo from "@assets/images/astro-logo-light-gradient.png";
import { Content as SampleMarkdown } from "@content/sample-markdown.md";
import { Image } from "astro:assets";
import RandomImage from '../components/RandomIcon.astro';
import RandomQuote from '../components/Randomrules.astro';
import RandomComp from '../components/RandomComp.astro';
import Timer from '../components/Timer.astro';
import RulesOverlay from '@components/RulesOverlay.astro';
import AccentColor from "@components/AccentColor.astro";
---

<Layout title="Astro Starter">
  <div class="page">
    <div class="titlequote">
      <h1 class="np"><span class="star">*</span>The New Paltz <strong>Constraint Lab.</strong></h1> 
       <div class="buttonimage">

   <RandomImage />
 <AccentColor />

 </div>
     
    </div>
  <div class="imagecomp">
    <RandomComp />
      <RandomQuote client:load/>

   
    </div>
    <div class="footer">
      <div class="tr-left">
        <Timer />
         <button id="rules-open-btn">Rules</button>
        <button onclick="location.reload()">Reroll</button>
      </div>
       
      <div class="tr-right">
      <a class="submit" href="https://forms.office.com/r/png4kFiEH7">Submit</a>"
      

    


 </div>
 
  </div>
</Layout>
<RulesOverlay />
<style>
  html, body { height: 100%; margin: 0; padding: 0; box-sizing: border-box; }
  *, *::before, *::after { box-sizing: inherit; }

  .star {
    color: var(--color-accent);
    font-weight: bold;
    line-height: 0.8;
    margin-right: 0.25rem;}
  }
  
  .submit {
    line-height: normal;
    border: 2px solid var(--color-accent);
    padding: 0.75rem 1rem;
    background-color: white;
    color: blue;
    font-size: clamp(1rem, 2.5vw, 1.25rem);
    width: fit-content;
    height: fit-content;
    border-radius: 20px;
  }
.tr-left {
    display: flex;
    flex-direction: row;
    gap: 1rem;
   width: fit-content;
    align-items: center;
  }

  .footer {
    display: flex;
    flex-direction: row;
    gap: 1rem;
    align-items: center;
    background-color: blue;
    width: 100%;
    justify-content: space-between;
    padding: 0.5rem 1rem;
  }

  .buttonimage {
    display: flex;
    flex-direction: row;
    gap: 1rem;
    height: fit-content;
    max-width: 30rem;
    width: 100%;
    align-items: flex-start;
  }

  .imagecomp {
    display: flex;
    justify-content: flex-start;
    align-items:flex-start;
    gap: 1rem;
    flex-direction: column;
  }

  .page {
    display: flex;
    flex-wrap: wrap;
    width: 100%;
    justify-content: center;
    gap: 1rem;
    margin:0;
    margin-top: 1.5rem;
    transition: all 0.25s ease;
  }

  h1 {
    line-height: .9;
    background-color: blue;
    color: white;
    border: 2px solid var(--color-accent);
    padding: 1rem;
    border-radius: 20px;
    font-size: clamp(2rem, 6vw, 5rem);
    max-width: 30rem;
    width: 100%;
    height: fit-content;
    transition: all 0.2s ease;
  }

  h1:hover {
    background-color: white;
    color: blue;
    border-radius: 30px;
    transform: scale(1.03);
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.12);
  }

  .titlequote {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: 1rem;
  }

  button {
    line-height: normal;
    border: 2px solid var(--color-accent);
    padding: 0.6rem 0.9rem;
    background-color: blue;
    color: white;
    font-size: clamp(0.9rem, 2.2vw, 1.1rem);
    width: fit-content;
    height: fit-content;
    border-radius: 12px;
  }

  button:hover {
    background-color: white;
    color: blue;
    transition: all 0.18s ease;
    border-radius: 18px;
    transform: scale(1.02);
    cursor: pointer;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.12);
  }
  a {
    line-height: normal;
    border: 2px solid var(--color-accent);
    padding: 0.75rem 1rem;
    background-color: white;
    border-radius: 18px;
    color: blue;
    font-size: clamp(1rem, 2.5vw, 1.25rem);
    width: fit-content;
    text-decoration: none;
  }
  a:hover {
    background-color: blue;
    color: white;
    transition: all 0.18s ease;
    border-radius: 20px;
    transform: scale(1.02);
    cursor: pointer;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.12);
    border: 1px solid white;
  }
  body {
    height: fit-content;
  }
</style>
<script>
  (function(){
    const openBtn = document.getElementById('rules-open-btn');
    const overlay = document.getElementById('rules-overlay');
    if (!openBtn || !overlay) return;
    const dialog = overlay.querySelector('.rc-dialog');
    const closeButtons = overlay.querySelectorAll('[data-overlay-close]');
    const backdrop = overlay.querySelector('[data-overlay-backdrop]');
    let previouslyFocused = null;

    function getFocusable(el){
      return Array.from(el.querySelectorAll('a[href],button:not([disabled]),input:not([disabled]),textarea:not([disabled]),select:not([disabled]),[tabindex]:not([tabindex="-1"])'))
        .filter(e => e.offsetWidth || e.offsetHeight || e.getClientRects().length);
    }

    function open(){
      previouslyFocused = document.activeElement;
      overlay.classList.add('open');
      overlay.setAttribute('aria-hidden','false');
      const focusable = getFocusable(dialog);
      (focusable[0] || dialog).focus();
      document.addEventListener('keydown', onKeyDown);
    }

    function close(){
      overlay.classList.remove('open');
      overlay.setAttribute('aria-hidden','true');
      if (previouslyFocused && previouslyFocused.focus) previouslyFocused.focus();
      document.removeEventListener('keydown', onKeyDown);
    }

    function onKeyDown(e){
      if (e.key === 'Escape') return close();
      if (e.key === 'Tab'){
        const focusable = getFocusable(dialog);
        if (focusable.length === 0) { e.preventDefault(); return; }
        const first = focusable[0];
        const last = focusable[focusable.length-1];
        if (!e.shiftKey && document.activeElement === last){ e.preventDefault(); first.focus(); }
        if (e.shiftKey && document.activeElement === first){ e.preventDefault(); last.focus(); }
      }
    }

    openBtn.addEventListener('click', open);
    closeButtons.forEach(b => b.addEventListener('click', close));
    backdrop.addEventListener('click', close);
  })();
</script>